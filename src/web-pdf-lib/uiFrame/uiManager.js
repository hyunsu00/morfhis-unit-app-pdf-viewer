export default (function () {
  return {
    hideLoadingProgress: function () {
      console.warn(`hideLoadingProgress() 구현 필요`);
    },

    showLoadingProgress: function () {
      console.warn(`showLoadingProgress() 구현 필요`);
    },
  };
})();
